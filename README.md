# TVF MERN – Client (React + Vite + Tailwind)

## Run
1. Set the API URL (optional): create `.env` with `VITE_API_URL=http://localhost:5000`
   - If omitted, it defaults to `http://localhost:5000`
2. Install & run:
   ```bash
   npm install
   npm run dev
   ```
